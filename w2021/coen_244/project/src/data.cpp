#include "graph.h"

bool Graph::readDatabase(string path) 
{


    // init the file var
    ofstream db;
    //open the file
    db.open(path);


    return 1;
}

bool Graph::addDatabase() 
{
    return 1;
}